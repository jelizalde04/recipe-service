{
  "swagger": "2.0",
  "info": {
    "title": "Recipe Modification Permission Microservice",
    "description": "API to check if a user has permission to modify a recipe",
    "version": "1.0.0"
  },
  "paths": {
    "/check-permission": {
      "get": {
        "summary": "Check if user can modify a recipe",
        "parameters": [{"name": "recipe_id", "in": "query", "required": true, "type": "integer"}],
        "security": [{"BearerAuth": []}]
      }
    }
  }
}
